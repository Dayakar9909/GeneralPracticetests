package ArrayDesignsAndPatterns;

import java.util.concurrent.TimeUnit;

public class DynamicRadioButton {

    Webdriver driver;

 /*   public static void main(String[] args) {
        try {
            System.setProperty("webdriver.chrome.driver", "c:/path");
            Webdriver driver = new Chromedriver();
            driver.manage().window().maximize();
            driver.manage().timeout().implicitlyWait(10, TimeUnit.SECONDS);
            driver.get("https://www.facebook.com/");

            driver.findElement(By.id("u_0_2")).click();

            Thread.sleep(3000);
            driver.close();
        } catch(Exception e){
            e.printStackTrace();
        }
      }

      public static void selectRadioButton(WebDriver driver, String value){
        WebElement element = driver.findElement(By.xpath("//span@class='_5k_2_5dba']/"));
        System.out.println("Before: " + element.isSelected());
        if(!(element.isSelected())){
            element.click();
        }
        System.out.println("After: " + element.isSelected());
      }
    }*/
}
